"""This service will handle stdio comms to the pandoc image."""

# This service will act as an comms bridge to speak to the pandoc image.
# The class should throw a pandoc error if the pandoc image does not respond

# Should this class be a decorator that is called within the other services?